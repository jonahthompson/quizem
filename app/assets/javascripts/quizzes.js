$(document).on('turbolinks:load', function(){
	// $('.next_question_button').on('click', function(){
	// 	let div = $('.question_form_wrap')
	// 	div.animate({right: '150%'}, "slow");
	// })
 let slideArray = $('.question_form_wrap');

	function questionCheck(){
		// let last_i = slideArray.length - 1;
		// for (var i = 0; i < slideArray.length; i++) {

		// }
		// slideArray[last_i].find('.button_holder').append(`<button type="submit">Button</button>`);
	// questionCheck(slideArray);
}
});